# 知屿 KnowIsle · 开发进度与目标

> 本文件是项目的**活文档**：每个迭代结束时更新进度表、记录决策、明确下一步。
> 产品与技术设计的唯一权威来源是《知屿-智能课程知识社区-项目文档.md》（v3.2.1），本文件只做进度跟踪与任务拆解，不重复设计内容。

## 更新记录

| 日期 | 版本 | 内容 | 记录人 |
| --- | --- | --- | --- |
| 2026-09-15 | v0.1 进行中 | 项目骨架 + Docker 容器化完成并验证通过；对象存储由 MinIO 迁移至 SeaweedFS（见文档 v3.2.1 修订说明） | D（数据与 DevOps） |
| 2026-09-15 | v0.1 完成 | 24 张表 ORM 模型 + 首个 Alembic 迁移（e2e8ae0dcb8e）落地；迁移正反向验证通过；CI 检查（ruff+pytest）容器内通过；推送 feature 分支 | D（数据与 DevOps） |
| 2026-09-15 | v0.1 完成 | Jinja2 基础布局完善：全站导航栏（板块入口+高亮）、通知未读角标（30s HTMX 轮询 + unread-count 接口）、六板块占位页、HTML 404 页（API 仍返回统一 JSON）；修复 /{page} 通配遮蔽 /healthz 的路由顺序问题；CI 触发条件修复（覆盖 feature 分支）；测试扩至 7 例全绿 | D（数据与 DevOps） |
| 2026-09-18 | v0.2 完成 | 认证（邮箱降级通道 + Redis Session/JWT 双通道）、RBAC 四角色门控、专业名单导入（脚本+管理接口）、课程空间结构（创建/申请/审批/章节树）全部落地；登录页两步流程、导航栏登录态、专业/课程卡片页、课程空间页联调通过；测试扩至 21 例全绿；容器内 E2E 全流程验证通过 | D（数据与 DevOps） |

## 一、总体里程碑（文档 §20.1）

| 里程碑 | 时间 | 目标 | 状态 |
| --- | --- | --- | --- |
| M1 v0.3 | 第 4 周末（计划 2026-10-11） | 骨架可用版：个人库 RAG 端到端可用 | ⬜ 未开始 |
| M2 v0.6 | 第 8 周末（计划 2026-11-08） | 社区闭环内测版 | ⬜ 未开始 |
| M3 v1.0 | 第 12 周末（计划 2026-12-06） | 答辩演示定稿版 | ⬜ 未开始 |

迭代节奏：每 2 周一个小版本（v0.1 ~ v1.0），每个小版本结尾必须可演示。

## 二、当前进度：v0.1（第 1-2 周）

**v0.1 验收标准**（文档 §20.3）：一条命令拉起全部服务；迁移可执行；CI 绿灯。

| 任务 | 状态 | 说明与证据 |
| --- | --- | --- |
| 项目骨架（按文档 §14 目录结构） | ✅ 完成 | `app/`（api/web/core/community/identity/moderation/workers/storage）、`client/`、`alembic/`、`deploy/`、`scripts/`、`tests/` 全部就位，模块 stub 均标注对应文档章节与交付版本 |
| Docker Compose 一键拉起 | ✅ 完成 | 7 容器（app/worker/postgres16/redis7/seaweedfs4.47/chroma1.0.15/nginx1.27）全部健康；`localhost:8080/healthz` 返回 `{"status":"ok"}` |
| 存储层连通性 | ✅ 完成 | aioboto3 对 SeaweedFS 实测建桶→写入→读回→删除；Chroma 心跳正常；PostgreSQL 16.15 连接正常；ARQ worker 已连接 Redis |
| CI（lint + pytest） | ✅ 完成 | 本地容器内 ruff + pytest（7 passed）通过；触发条件已修复覆盖 feature 分支并推送，GitHub Actions 绿灯待页面最终确认 |
| Jinja2 基础布局 | ✅ 完成 | 全站导航栏（五板块入口+当前页高亮）、通知未读角标（30s 轮询）、六板块占位页、HTML 404 页；首页板块总览卡片 |
| 数据库 DDL + Alembic 迁移 | ✅ 完成 | `app/storage/models.py` 24 张表 ORM 模型（含全部 CHECK 约束、唯一约束、部分索引）；迁移 `e2e8ae0dcb8e` 生成并验证：upgrade 后 24 张表就位、downgrade/upgrade 循环可逆、随 app 容器启动自动执行 |

**结论：v0.1 全部完成 ✅（骨架 / Docker / 数据库迁移 / CI / Jinja2 基础布局），可进入 v0.2。**

### 当前环境现状

- 入口：http://localhost:8080 （nginx → app）；SeaweedFS Filer 调试界面：http://localhost:8888
- 应用镜像 `knowisle-app:latest`（约 1.47GB，含 LibreOffice 与中文字体，供 Office→PDF 预览转换）
- 待填配置：`.env` 中 `OFFICIAL_LLM_API_KEY`（DeepSeek）、`COLLEGE_NAME`
- 镜像版本全部固定（不用 latest），保证团队环境一致

## 三、下一步行动（启动 v0.3：个人库全链路）

| # | 任务 | 建议负责人 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | CI 远端验证（feature 分支推送后观察 Actions） | D | GitHub Actions 绿灯 | ⬜ 待观察 |
| 2 | 申请 DeepSeek API Key 填入 `.env` | A | `docker compose restart app worker` 后配置生效 | ⬜ 待办 |
| 3 | 接口契约文档初始化（抄录文档 §12.1 接口总表到共享文档） | B | 全员可查，后续变更走 PR 评审 | ⬜ 待办 |
| 4 | 文档上传 + 解析 + 切块 + 向量化（ARQ 链路，模块 A1/A2） | A | 上传真实课件 30s 后可问答 | ⬜ v0.3 主线 |
| 5 | SSE 流式问答 + 引用溯源（`/api/chat`） | A | 逐字渲染 + chunk_id 引用 | ⬜ v0.3 主线 |
| 6 | 个人知识库页（/library） | C | 学期/课程/章节树 + 上传进度轮询 | ⬜ v0.3 主线 |

遗留运营项：git 保护分支设置与成员邀请、生产部署前将 `EMAIL_CODE_ECHO` 置为 false。

## 四、当前进度：v0.2（第 3-4 周前半）——已完成 ✅

**内容**（文档 §20.3）：认证（邮箱降级）、RBAC、专业名单导入、课程空间结构。
**验收标准**：可登录、角色权限生效、空间可创建——**三条全部达成**（2026-09-18 容器内 E2E 验证）。

### 完成情况与证据

| 模块 | 任务 | 状态 | 说明与证据 |
| --- | --- | --- | --- |
| 认证 | 邮箱降级通道（学号 + 校园邮箱验证码）+ Redis Session + JWT 双通道 | ✅ | `identity/email_fallback.py` / `identity/session.py` / `api/auth.py`；E2E：发码（dev_code 回显）→ 验证 → 会话 Cookie → `/api/users/me` 200；`POST /api/auth/token` 签 JWT，Bearer 访问通过、篡改令牌 401；登出后会话即失效 |
| 认证 | CAS/OIDC 抽象层（仅接口预留，不对接） | ✅ | `identity/cas.py` 抽象 + `/api/auth/cas/login|callback` 路由预留（501），切换仅改 `AUTH_PROVIDER` |
| 权限 | RBAC 四角色门控依赖 + 数据级归属过滤（越权 404） | ✅ | `identity/rbac.py`（reviewer/builder 平行角色、admin 放行）；E2E：学生调管理接口 404、学生建章节 404、未登录 401、pending 课程学生 404 / 管理员 200 |
| 空间 | `scripts/init_majors.py` 专业名单 CSV 批量导入（幂等） | ✅ | 容器内执行导入 5 个专业；另有 `POST /api/admin/majors` 管理接口（写审计日志）；示例 `scripts/majors.example.csv` |
| 空间 | 课程空间创建/列表/章节树 API + 课程空间页 | ✅ | 管理员创建直接 active、学生申请 pending（`POST /api/courses`）、管理员审批 `PATCH /api/admin/courses/{id}`（审计留痕）；章节树 GET/POST；页面 /majors → /majors/{id} → /courses/{id}（章节树侧边栏 + 四 Tab 占位） |
| 初始化 | `scripts/create_admin.py` 管理员初始化 | ✅ | 幂等（已存在则提升为 admin），绑定 email_fallback 身份与登录邮箱 |
| 前端 | 登录页联调、导航栏登录态、专业/课程卡片页 | ✅ | 登录页两步 Alpine.js 流程（发码 → 验证，新用户第三步补全资料建档）；导航栏已登录显示昵称+退出；专业/课程卡片页真实数据渲染 |

### v0.2 关键决策（2026-09-18，与项目 Owner 确认）

1. **管理员人工核验**：验证码通过即建档登录，核验作为运营层面事后抽查；不改 24 表数据模型、不新增迁移、不偏离文档 §8 DDL。
2. **首次登录建档**：邮箱通道拿不到学籍信息，新用户在登录流程第三步补全（真实姓名/昵称/年级/专业）后建档；真实姓名仅核验用，接口与页面均不下发。
3. **验证码假通道**：开发期写 app 容器日志 + 接口回显 `dev_code`（`EMAIL_CODE_ECHO=true`，生产关闭）；验证码「先校验、登录/建档成功才销毁」，避免 need_profile 中间态吞码；重发冷却 60s。
4. **测试策略**：CI（无 Postgres/Redis）只跑内存替身的单元测试（FakeRedis/假用户对象）；DB/Redis 相关集成验证在 compose 容器环境内执行（本表 E2E 证据即由此产生）。

**结论：v0.2 全部完成 ✅，可进入 v0.3（个人库全链路）。**

## 五、协作规则速查（文档 §19）

- **分支**：`main` 保护 + `feature/xxx` + PR（≥1 人 review，CI 绿才合）
- **Commit**：Conventional Commits（`feat:` / `fix:` / `refactor:` / `test:` / `docs:`）
- **DoD**：代码合入 main + 接口契约文档同步 + 单测通过 + Owner 在迭代评审演示
- **关键模块双人制**：`core/pipeline.py`、`moderation/workflow.py` 必须 Owner + Backup 两人可维护
- **节奏**：每周一次站会，每两周一次迭代评审；任务粒度 ≤ 3 天；每个迭代预留 20% 缓冲

## 六、常用命令速查

```bash
# 拉起 / 停止（项目根目录执行）
docker compose -f deploy/docker-compose.yml up -d --build
docker compose -f deploy/docker-compose.yml down          # 保留数据
docker compose -f deploy/docker-compose.yml down -v       # 清空数据（慎用）

# 日志与排障
docker compose -f deploy/docker-compose.yml ps
docker compose -f deploy/docker-compose.yml logs -f app   # worker / nginx 同理

# 数据库迁移（容器内执行）
docker compose -f deploy/docker-compose.yml exec app alembic revision --autogenerate -m "描述"
docker compose -f deploy/docker-compose.yml exec app alembic upgrade head

# 初始化（容器内执行，v0.2 起可用）
docker compose -f deploy/docker-compose.yml exec app python scripts/create_admin.py \
  --student-no 20230001 --nickname 管理员 --email admin@stu.example.edu.cn
docker compose -f deploy/docker-compose.yml exec app python scripts/init_majors.py scripts/majors.example.csv

# 质量检查（与 CI 一致，容器内跑，不污染本机）
docker run --rm knowisle-app:latest sh -c \
  "pip install -q -r requirements-dev.txt; ruff check app tests scripts && pytest -q"
```

## 七、风险登记（随迭代更新）

| 风险 | 状态 | 应对 |
| --- | --- | --- |
| DeepSeek API Key 未申请，AI 功能无法联调 | 🔴 待办 | 本周完成（下一步任务 4），预算 <¥100 |
| 种子资料冷启动 | 🟡 关注 | 第 3 周起 4 名成员各自上传真实课件/笔记，W8 前每人 ≥5 份、覆盖 ≥2 门课 |
| CAS 对接受阻 | 🟢 已兜底 | 邮箱降级通道为开发期默认（文档 §3.1），v0.2 已落地 |
| 验证码假通道被带到生产 | 🟡 关注 | `EMAIL_CODE_ECHO` 默认 true 仅为开发便利，生产部署清单加入「置为 false」检查项 |
| 12 周工期紧张 | 🟢 有护栏 | 范围护栏条款（文档 §20.4）：先砍 P2 再简化 P1，P0 不动 |
